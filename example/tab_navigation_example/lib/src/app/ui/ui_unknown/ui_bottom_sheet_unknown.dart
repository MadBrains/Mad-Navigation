import 'package:tab_navigation_example/src/app/app.dart';

class UiBottomSheetUnknown extends StatelessWidget {
  const UiBottomSheetUnknown({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Bottom sheet not found'));
  }
}
