import 'package:root_navigation_example/src/app/app.dart';

class UiDialogTest extends StatelessWidget {
  const UiDialogTest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
